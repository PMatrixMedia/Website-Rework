import Contact from "../Pages/Contact/contact";

export const metadata = {
  title: "Contact | PhaseMatrix Media",
  description: "Get in touch with PhaseMatrix Media.",
};

export default function ContactPage() {
  return <Contact />;
}
