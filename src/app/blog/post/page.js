import Post from "../../Pages/Blog/post";

export const metadata = {
  title: "New Post | PhaseMatrix Blog",
  description: "Create a new blog entry",
};

export default function PostPage() {
  return <Post />;
}
