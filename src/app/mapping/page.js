import MappingClient from "./MappingClient";

export const metadata = {
  title: "Globalmapping | PhaseMatrixMedia",
  description: "3D globe — PhaseMatrixMedia",
};

export default function MappingRoutePage() {
  return <MappingClient />;
}
